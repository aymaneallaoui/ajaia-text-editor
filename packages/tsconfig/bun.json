{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Bun (API / server)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["bun"],
    "allowImportingTsExtensions": true
  }
}
